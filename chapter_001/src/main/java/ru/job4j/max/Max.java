package ru.job4j.max;
/**
 * Класс Mac c методом max для вычислния большего из двух.
 * @author   AVasiliev
 * @since    07.12.2017
 * @version  1.0.0
 */

// Странно: почему-то НЕ отправляется.

public class Max {

   public int max(int first, int second) {
      return (first >= second) ? first : second;
   }
}